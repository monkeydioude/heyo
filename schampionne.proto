syntax = "proto3";

package grpc;

service Broker {
  rpc Whisper (Rumor) returns (Ack) {}
  rpc Listen (Listener) returns (stream Rumor) {}
}

message Rumor {
  string type = 1;
  string message = 2;
}

message Listener {
    string type = 1;
}

message Ack {
    string message = 1;
    AckCode code = 2;
}

enum AckCode {
  OK = 0;
  NO_LISTENER = 1;
}